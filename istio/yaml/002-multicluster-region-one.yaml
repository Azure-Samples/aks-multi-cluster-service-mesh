apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  values:
    global:
      meshID: azure
      multiCluster:
        clusterName: aks-cluster-one
        enabled: true
      network: network1
