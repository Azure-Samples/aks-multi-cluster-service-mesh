apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  values:
    global:
      meshID: azure
      multiCluster:
        clusterName: westeurope-aks
        enabled: true
      network: network1
